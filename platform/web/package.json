{
  "name": "switch-emu-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Web platform layer for the Voland C core. WASM-first.",
  "engineStrict": true,
  "engines": {
    "pnpm": ">=9.0.0",
    "node": ">=22.6"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test",
    "e2e": "playwright test"
  },
  "dependencies": {
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@webgpu/types": "^0.1.69",
    "typescript": "^6.0.3",
    "vite": "^5.2.0",
    "vite-plugin-solid": "^2.10.0"
  }
}
